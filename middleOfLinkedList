Node *findMiddle(Node *head) {
    // Write your code here
    vector<Node*> nodes;
    while(head!=NULL){
        nodes.push_back(head);
        head=head->next;
    }
    
    return nodes[nodes.size()/2];
}
